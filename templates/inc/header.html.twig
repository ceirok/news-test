<header class="header">
    <div class="container">
        <div class="nav--container">
        <a href="#" class="logo"><h1>Logo</h1></a>
            <nav class="nav">
                <ul class="nav--menu">
                    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                        <li class="nav--menu__item"><a href="#">Log out</a></li>
                        <li class="nav--menu__item"><a href="/add">Add new article</a></li>
                    {% else %}
                        <li class="nav--menu__item"><a href="{{ path('login') }}">Log in</a></li>
                        <li class="nav--menu__item"><a href="{{ path('user_registration') }}">Sign up</a></li>
                    {% endif %}
                </ul>
            </nav>
        </div>
    </div>
</header>